#include <stdio.h>


int main(Void)
//opening bracket should not disappear
{
    // INT should be in all lowecase
    int sum;
    // incomplete set for commenting, should end in */
    /* COMPUTE RESULT */
    // semicolon is very important!
    sum = 25 + 37 - 19;
    // wrong way of commenting, should be */ not //
    /* DISPLAY RESULTS */
    // should have comma before the variable
    printf ("The answer is %i\n", sum);
    return 0;
}
